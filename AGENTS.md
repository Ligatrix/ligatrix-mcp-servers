# Ligatrix MCP Servers Instructions

SEAL_STATUS: SEALED

This public repository contains MCP-style server examples.

Do not add:

- private file readers;
- credentials;
- client data;
- hidden network calls;
- tools that modify files without explicit inputs.

Keep tools deterministic and safe by default.

